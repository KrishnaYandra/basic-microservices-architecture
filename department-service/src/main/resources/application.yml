server:
  port: '9001'

spring:
  boot:
    admin:
      client:
        url:
        - http://localhost:9000
  application:
    name: DEPARTMENT-SERVICE
#  zipkin:
#    base-url: http://localhost:9411/

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

