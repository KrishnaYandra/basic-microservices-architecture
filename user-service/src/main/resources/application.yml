server:
  port: '9002'

spring:
  boot:
    admin:
      client:
        url:
        - http://localhost:9000
  application:
    name: USER-SERVICE
#  zipkin:
#    base-url: http://localhost:9411/

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always